import 'package:flutter/material.dart';

class AppColors {
  // Main background (Parchment/Cream)
  static const Color background = Color(0xFFF8F1D4);
  
  // Cards and secondary surfaces
  static const Color surfacePrimary = Color(0xFFF6EFCF);
  static const Color surfaceSecondary = Color(0xFFFFF8E7);
  
  // Primary buttons & Brand colors (Maroon/Burgundy)
  static const Color primaryButton = Color(0xFF5A0018);
  
  // Active states
  static const Color activeState1 = Color(0xFF4A0012);
  static const Color activeState2 = Color(0xFF6B001B);
  
  // Highlights and Accents
  static const Color highlight = Color(0xFF7A001F);
  static const Color accentGold = Color(0xFFC5A059); // Muted gold accent
  
  // Borders
  static const Color border = Color(0xFF800020);
  static const Color divider = Color(0xFFD6C8A1);
  
  // Destructive actions
  static const Color destructive = Color(0xFF4A0012);
  static const Color error = Colors.redAccent;
  
  // Text
  static const Color textPrimary = Color(0xFF2C1919); // Deep dark brown/maroon instead of pure black
  static const Color textSecondary = Color(0xFF604949);
  
  // Room Status Colors
  static const Color statusAvailable = Color(0xFFF8F1D4);
  static const Color statusReserved = Color(0xFFC5A059); // Muted gold
  static const Color statusOccupied = Color(0xFF5A0018); // Maroon
  static const Color statusMaintenance = Color(0xFF8C7D7D); // Muted greyish brown
}
